[package]
name = "echo-node"
version = "0.1.0"
edition = "2021"
description = "forge lab 01 — idempotent receiver over a hostile network (byzantine)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
