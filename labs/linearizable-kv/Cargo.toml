[package]
name = "linearizable-kv"
version = "0.1.0"
edition = "2021"
description = "forge lab 06 — linearizable KV on Raft (byzantine)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
